# Specifies intentionally untracked files to ignore in Git
.env
.env.local
.env.*.local

/bin/
*.out
*.test
coverage.*

# go build ./cmd/<tool> with no -o drops the binary in the repo root
/loadtest
/targetsrv

/tmp/

.DS_Store
